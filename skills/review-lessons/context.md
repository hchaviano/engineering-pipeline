<!--
Fill when adopting. Leave no <placeholders>.
Lesson area ids and where to promote conventions belong here — not in SKILL.md.
-->
# Review lessons — project context

## Catalog

- File: [lessons.md](lessons.md) in this folder
- Status values: `active` (follow) · `promoted` (also inlined in a standards file — keep the entry so sources stay findable)

## Apply when

| Stage | What to take from the catalog |
|-------|-------------------------------|
| Specify / Architecture Validation | `<e.g. state-management twins, routing go vs push>` |
| Code (tests + production) | All matching lessons |
| Self-review | Treat violations in **new/changed** code as 🟡 Nit (unless the review rubric says otherwise) |

## Area ids

`<e.g. ARCH, NAV, UI, LISTEN, TEST, DOCS>` — add/remove to match the project.

Id shape: `L-{AREA}-{n}`

## Promote to

When a lesson is now a durable convention:

- Architecture / state / routing / tests / UI → `<standards-file e.g. CLAUDE.md>`
- Self-review always-check nits → `<review rubric e.g. REVIEW.md>`
